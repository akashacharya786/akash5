#include<stdio.h>
void main()
{
   int i,n,sum=0,a;
   printf("enter the value of n \n");
   scanf("%d",&n);
   for(i=0;i<n;i++)
   if(i%2==0)
    {
       sum=sum+i;
    }
    printf("%d",sum);
   if(sum%3==0)
    {
     printf("the number %d is divisible by 3 \n",sum);
    }
   else
    {
     printf(" the number %d is divisible by 3\n",sum);
    }
   
}
    
